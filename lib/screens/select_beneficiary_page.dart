import 'package:flutter/material.dart';

class SelectBeneficiary extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("SELECT BENEFICIARY"));
  }
}
